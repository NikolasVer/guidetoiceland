import React from "react";
import { shallow, mount } from "enzyme";
import "../../setupTests";
import PhotosSection from "./index";

describe('Render tests', () => {
  const component = shallow(<PhotosSection count={10} />);

  it('test', () => {
    
  })
});